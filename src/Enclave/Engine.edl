enclave {
    
    from "sgx_tsgxssl.edl" import *;

    untrusted {
        void ocall_print_string([in, string] const char *str);
    };


    trusted {
        public void teste_ecall();
 
    };
};
